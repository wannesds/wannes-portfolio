import React from 'react';

const Viewlist = ({type}) => {
    return(
        <div className="viewlist">
            <p>view items of {type}</p>
            {
               // list.map(())
            }
        </div>
    )
}

export default Viewlist;